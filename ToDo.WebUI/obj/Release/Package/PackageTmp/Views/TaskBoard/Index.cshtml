@{
    Layout = "~/Views/Shared/Layout.cshtml";
}
@{
    ViewBag.Title = "Task Board";
}

<section class="row">
    <section class="col-md-12">
        <h1>Task Board Page</h1>
    </section>
</section>
<section class="row">
    <section class="col-md-3">

        <section class="panel panel-default text-center">
            <section class="panel-heading">
                <h4> ToDo</h4>
            </section>
            <section class="panel-body" data-bind=" event:{
                                      dragover: function(event){console.log('dragover'); },
                                     dragenter: function(event){console.log('dragenter'); },
                                     dragleave: function(event){console.log('dragleave'); },
                                     drop: function(data, event){
                                         event.target.id = 1; 
                                         event.target.action = 'change-status' ;  
                                         $root.ondrop($data, event); return true;
                                     }}">

                <section data-bind="foreach: tasks">

                    <section data-bind="if: Status === 1">
                        <section class="well" draggable="true" data-bind="event:{
dragstart: function(data, e) {$root.ondragstart(data, e); return true;}
}">
                            <section data-bind="text: Title"></section>
                            <section data-bind="date: DueDate, format: 'DD MMM YYYY'"></section>
                        </section>
                    </section>

                </section>


            </section>
        </section>

    </section>
    <section class="col-md-3">

        <section class="panel panel-default text-center">
            <section class="panel-heading">
                <h4> Inprogress</h4>
            </section>
            <section class="panel-body" data-bind=" event:{
                                      dragover: function(event){console.log('dragover'); },
                                     dragenter: function(event){console.log('dragenter'); },
                                     dragleave: function(event){console.log('dragleave'); },
                                     drop: function(data, event){ 
                                         event.target.id = 2; 
                                         event.target.action = 'change-status' ; 
                                         $root.ondrop($data, event); 
                                         return true;
                                     }}">
                <section data-bind="foreach: tasks">

                    <section data-bind="if: Status === 2">
                        <section class="well" draggable="true" data-bind="event:{
dragstart: function(data, e) {$root.ondragstart(data, e); return true;}
}">
                            <section data-bind="text: Title"></section>
                            <section data-bind="date: DueDate, format: 'DD MMM YYYY'"></section>
                        </section>
                    </section>

                </section>

            </section>
        </section>

    </section>
    <section class="col-md-3">

        <section class="panel panel-default text-center">
            <section class="panel-heading">
                <h4> Finished</h4>
            </section>
            <section class="panel-body" data-bind=" event:{
                                      dragover: function(event){console.log('dragover'); },
                                     dragenter: function(event){console.log('dragenter'); },
                                     dragleave: function(event){console.log('dragleave'); },
                                     drop: function(data, event){
                                         event.target.id = 3;
                                         event.target.action = 'change-status' ;
                                         $root.ondrop($data, event);
                                         return true;              
                                     }}">
                <section data-bind="foreach: tasks">

                    <section data-bind="if: Status === 3">
                        <section class="well" draggable="true" data-bind="event:{
dragstart: function(data, e) {$root.ondragstart(data, e); return true;}
}">
                            <section data-bind="text: Title"></section>
                            <section data-bind="date: DueDate, format: 'DD MMM YYYY'"></section>
                        </section>
                    </section>

                </section>

            </section>
        </section>

    </section>

    <section class="col-md-3">
        <section>
            <section>
                <input type="text" data-bind="value: TaskTitel" />
            </section>
            <section>
                <textarea data-bind="value: TaskDescription" rows="5" cols="45"></textarea>
            </section>
            <section>
                <input type="button" value="Add task" data-bind="click: onAddTask" />
            </section>
        </section>

        <section id="trash" class="trash-box" data-bind="event:{
     dragover: function(event){$root.ondragoverTrash(); },
     dragenter: function(event){ $root.ondragenterTrash();},
     dragleave: function(event){ $root.ondragleaveTrash();},
            drop: function(data, event) {
                 event.target.action = 'remove' ;
                 $root.ondrop($data, event);
                 return true;
            }}"></section>

    </section>

    </section>
@*</section>
    <section class="row">

        <section class="col-md-3 col-md-offset-9 trash-box" data-bind="event:{
                drop: function(data, event) {
                     event.target.action = 'remove' ;
                     $root.ondrop($data, event);
                     return true;
                }}"></section>

    </section>*@
